# Seeform Product Contract

## Positioning

Seeform（见相）是面向 AI Agent 的视觉判断与转化 Skill。

它把用户自然语言中的视觉意图转化为：

```text
看懂照片 -> 判断问题与潜力 -> 给出视觉观点 -> 选择最小有效改造 -> 调用编辑能力 -> 对比结果 -> 记录用户决定
```

Seeform 的长期目的，是让 Agent 理解用户想呈现什么，判断照片哪里没有成立，决定应改变多少，并在用户一次次选择后逐渐理解这个人的视觉偏好。

Seeform 不以“自动修图”作为核心定位。模型负责生成，编辑器负责执行，Seeform 负责理解、判断、提案、约束、比较和反馈闭环。

## User Surface

用户入口必须直接、简短、低负担。用户可以只提供：

- 一张照片；
- 一句想表达的感觉，例如“人物好看一点”“真实但更有状态”；
- 一个用途，例如“适合发小红书”。

Seeform 应优先给出一个简短、可理解的视觉观点和默认方案，而不是把内部对象、编辑参数或摄影术语变成问卷。

示例：

> 这张照片人物状态本来不错，主要问题是背景比人物更抢眼。建议保留人物真实特征，压低背景竞争，再做轻微提亮和肤色协调，适合小红书发布。我先按这个方向处理。

只有以下情况才需要用户补充或确认：

- 真实性模式无法确定；
- 用户的要求存在不可兼容的方向；
- 改动可能影响人物身份、场景事实或公开发布风险；
- 当前素材或执行能力不足以完成目标。

确认应尽量压缩为一次简单选择，例如“按这个方向”“更自然”“更出片”，而不是多轮参数访谈。

## Product Domain

后台保留完整分层，但不要求用户理解它：

1. **Visual Intent**：用户想表达什么、用于什么场景、允许和禁止什么改变。
2. **Source Fact**：从原片直接确认的事实和不确定性。
3. **Visual Diagnosis**：主问题、潜力、可恢复性和改造等级。
4. **Transformation Candidate**：有策略、来源、操作、hash、父候选和风险边界的候选结果。
5. **User Decision**：用户接受、拒绝、要求调整及其自然语言反馈。

这些对象构成内部接口。用户看到的是观点、默认方向、候选差异和一次轻量确认。

## Difference From Generic AI Photo Editing

普通 AI 修图通常从一个动作或效果开始：美颜、调色、磨皮、换背景、扩图、消除或套用风格。它的主要价值是快速执行既定操作。

Seeform 的差异在于：

| 普通 AI 修图 | Seeform |
|---|---|
| 从功能或滤镜开始 | 从用户想表达什么开始 |
| 直接给出一个结果 | 先解释照片哪里没有成立 |
| 默认采用平台审美 | 根据照片、用途和用户语言形成观点 |
| 主要比较“改前/改后” | 比较不同视觉策略及其代价 |
| 模型或工具隐含决定改多少 | 明确约束允许改变、必须保留和停止条件 |
| 用户反复试参数 | 用户用一句话确认或纠正方向 |
| 反馈通常只影响当前操作 | 反馈可形成可解释的个人视觉偏好 |
| 质量常被简化成更白、更亮、更清晰或更像模板 | 同时评价表达完成度、人物可信度、光线、材质、场景关系和真实性 |

Seeform 不声称底层编辑能力天然比成熟影像产品更强。它的产品价值在于视觉决策、能力编排、候选解释和用户反馈的连续性。

## V1 Focus

当前产品聚焦中国市场的普通照片与人像表达，尤其是：

- 人物好看但照片没有成立；
- 想显得精神、有状态、有质感，但不想明显变成另一个人；
- 需要适配小红书等社交媒体用途；
- 需要在自然、可信和更出片之间快速找到平衡。

插图、截图、视频帧、封面和概念视觉仍可作为未来兼容范围，但暂不作为当前主要验收目标。

默认基线仍为 **自然真实编辑感（Clean Natural Editorial）**。

产品允许路由的人像优化包括肤色、气色、局部瑕疵、面部光影、轻微五官比例和妆感优化；具体是否可执行取决于现场 Adapter，当前内置运行时不包含真实人像精修、对象 mask、面部编辑或生成式人像能力。禁止身份替换、基本五官拓扑大改、塑料皮肤、脸身光线冲突、明显年龄重写或生成场景事实。核心红线是人物、材质、光线和场景关系不能失真。

## Architecture Direction

系统分为四个深模块：

- **Intent Interpreter**：把一句感觉或用途转成视觉目标和最小必要约束。
- **Visual Director**：基于原片事实形成问题判断、潜力判断和少量策略观点。
- **Transformation Router**：把获准策略路由到确定性编辑、对象级编辑、人像编辑或生成式 Adapter。
- **Preference Learner**：只从明确的用户决定中提取可解释偏好；跨多个任务稳定重复后才晋升为长期偏好。

`V0-V3` 仍是运行时能力门禁，不是用户产品叙事：

- V0 文件访问；
- V1 视觉观察；
- V2 图像编辑；
- V3 工程比较。

底层 Adapter 必须可替换。Seeform 不默认自建成熟影像产品已经提供的抠图、分割、磨皮、背景填充或生成式编辑能力。

## Success Criteria

一个有效的 V1 闭环应满足：

```text
一句用户意图 -> 一次简短观点 -> 一个默认方案 -> 至多两个有明确差异的候选 -> 一次用户决定
```

当前验证重点：

- 用户是否认为 Seeform 说中了照片的问题；
- 用户是否能理解候选之间的差异；
- 用户是否愿意用一句话确认或纠正；
- 用户是否愿意再次使用；
- 用户反馈能否在多个案例中形成稳定、可解释的偏好；
- 结果是否同时满足用途、表达完成度和整体可信度。

技术检查、hash、模型分数和工程比较只能证明执行事实，不能单独证明视觉质量或用户接受。

## Out of Scope For Now

- 一键磨皮、变白、变瘦或默认改脸；
- 自建完整图像生成模型；
- 复杂的全自动审美评分器；
- 将一次反馈直接晋升为永久偏好；
- 同时把所有照片、插图、视频和设计素材作为等权验收目标；
- 用更多契约字段替代真实用户任务验证。
